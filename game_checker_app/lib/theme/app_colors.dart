import 'package:flutter/material.dart';

class AppColors {
  // Warna dari Figma (tambahkan 0xFF di depan kode Hex agar terbaca oleh Flutter)
  static const Color background = Color(0xFF181C27);
  static const Color primaryAccent = Color(0xFF404DFF); // Untuk tombol Telusuri
  static const Color bottomNav = Color(0xFF0F172A);

  // Kita tambahkan warna teks standar untuk dark mode
  static const Color textPrimary = Colors.white;
  static const Color textSecondary = Colors.grey;
}
